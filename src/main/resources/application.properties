spring.application.name=Bookshelf
server.port=${PORT:8080}
spring.datasource.url=jdbc:postgresql://${PGHOST:localhost}:${PGPORT:5437}/${PGDATABASE:bookshelfdb}
spring.datasource.username=${PGUSER:postgres}
spring.datasource.password=${PGPASSWORD:postgres}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

jwt.secret=${JWT_SECRET:bookshelf-api-chave-secreta-super-longa-para-assinar-tokens-jwt-2026}


